{
  "name": "@pipact/backend",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "prebuild": "npm run build -w @pipact/shared --if-present",
    "build": "tsc -p ."
  },
  "dependencies": {
    "@pipact/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^18.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
