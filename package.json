{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm run build -w @pipact/shared && npm run build -w @pipact/backend"
  },
  "dependencies": {
    "@vercel/node": "^5.5.15"
  }
}
